@model bool

@if (!Model)
{
    <div class="alert alert-warning">
        <span class="glyphicon glyphicon-alert"></span>
        <p>
            <strong>Переводчик не работает - нет соединения с Яндексом!</strong>
        </p>
    </div>
}