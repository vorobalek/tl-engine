@model IEnumerable<ExtCore.Infrastructure.IExtension>
@{
    ViewData["Title"] = "Index";
}

<div class="jumbotron">
    <h1 class="display-4">Добро пожаловать!</h1>
    <p class="lead">Поздравляем с запуском TL Engine Enterprise!</p>
    <hr class="my-4">
    <p>
        Список доступный модулей TL Engine:
        <ul>
            @foreach(var item in Model)
            {
                <li>@item.Name (@item.Description) - version @item.Version by @item.Authors @item.Url</li>
            }
        </ul>
    </p>
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="~/Account" role="button">Go to Account</a>
    </p>
</div>
